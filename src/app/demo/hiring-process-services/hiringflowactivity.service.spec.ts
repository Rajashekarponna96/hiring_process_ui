/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { HiringflowactivityService } from './hiringflowactivity.service';

describe('Service: Hiringflowactivity', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [HiringflowactivityService]
    });
  });

  it('should ...', inject([HiringflowactivityService], (service: HiringflowactivityService) => {
    expect(service).toBeTruthy();
  }));
});
